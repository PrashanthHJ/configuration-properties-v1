export const retrieveAllKeys = async (): Promise<string[]> => {
  // Simulate retrieving all keys from an object store
  return ['key1', 'key2'];
};

export const removeKey = async (key: string): Promise<void> => {
  // Simulate removing a key from an object store
};